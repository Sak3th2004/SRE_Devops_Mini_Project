global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: health-dashboard
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:5000
